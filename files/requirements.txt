# バージョンを固定しておくこと。
# 固定しないと、タグをpushしたタイミングでライブラリが勝手に新しくなり、
# 「昨日までビルドできていたのに今日は動かない」という事故が起きる。
# 更新したい場合は、手元で pip install -U してから動作確認し、この数字を書き換える。
customtkinter==5.2.2
uiautomator2==3.5.2
pygetwindow==0.0.9
pyautogui==0.9.54
adbutils==2.12.0
Pillow==12.1.0
