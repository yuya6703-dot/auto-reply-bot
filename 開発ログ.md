# 開発ログ

## このファイルは何か

作業のたびに起きた**エラーの実物**・**直した内容**・**突き止めるためにやったこと**・
**チャットで決めたこと**を、あとから読み返せるように積んでいく記録。

会話そのものは消えるし、読み返すのはトークンを食う。このファイルを最初に読めば
「前に何があって、なぜ今この作りなのか」が分かる状態にしておくのが目的。

### 書かないこと

- **機能の詳しい仕様は書かない。** それはコミットメッセージに既に長く書いてある
  （`git show <ハッシュ>` で読める）。ここに写すと二重管理になって必ずズレる。
- 一般的なプログラミングの説明。この製品で実際に起きたことだけを書く。

### 書くこと

| 項目 | 中身 |
|---|---|
| 症状 | 画面やログに実際に出た文言。推測ではなく実物 |
| 原因 | なぜそうなったか |
| 直した内容 | 何をどう変えたか（該当箇所の行番号は変わるので関数名で書く） |
| 突き止めた手段 | どう調べて原因に辿り着いたか。次に似た症状が出たとき同じ手が使える |
| 決めたこと | チャットで選んだ方針と、**選ばなかった案とその理由** |

### 追記のしかた

- 一番下に追記する。**過去のエントリは書き換えない**（間違いが分かった場合は
  新しいエントリで「訂正」として書く）。
- 1回の作業＝1エントリ。リリースしたらバージョンを見出しに入れる。

---

## 索引

| 日付 | 版 | 内容 | コミット |
|---|---|---|---|
| 2026-08-03 | v1.2.0 | MuMuのadb接続先をMuMuManagerに問い合わせる方式へ | `782f9f3` |
| 2026-08-07 | v1.3.0 | 画面(ディスプレイ)の取り違え対策、自動アップデート | `14926cc` |
| 2026-08-09 | v1.4.0 | 読み上げの出力デバイス選択 | `ec0e489` |
| 2026-08-13 | v1.4.1 | 出力デバイスのホストAPIをMMEへ修正 | `9603faa` |
| 2026-08-13 | v1.5.0 | スマホのマイクで話す (`/mic`)、HTTPS配信 | `1b95675` |
| 2026-08-14 | v1.6.0 | 感嘆「！」の自動判定 | `ab75f7b` |
| 2026-09-25 | v1.7.0 | キーワード自動返信のON/OFF ＋ 手入力送信（どちらもキーワード設定タブ） | 未コミット |

※ v1.1.0以前は記録なし。このファイルは2026-09-25に作り始めたため、
それ以前の分はコミットメッセージから起こした索引だけ。

---

## 2026-09-25 — キーワード自動返信のON/OFF ＋ 手入力して送る (v1.7.0)

### 何がしたかったか（チャットでの経緯）

> Windowsでコピペしたのをエミュグラビティに送信するため。楽したいから。

要望は2つ。

1. キーワードの自動入力（自動返信）をオン/オフするスイッチ
2. ツールに文字を打ち込んで、それをエミュレータ内のGRAVITYへ送る入力欄

本人の言葉で **「後者は前者が無いと干渉するから必要になった」**。
これは正しい読みだった（下の「潜んでいた事故」参照）。

### 決めたこと（と、選ばなかった案）

| 論点 | 決めたこと | 選ばなかった案と理由 |
|---|---|---|
| 停止中でも送れるようにするか | **監視中だけ送れる** | 停止中も送る案は、MuMu検出〜adb接続〜画面特定を監視ループの外にもう一系統作ることになる。実装量と検証の手間が増える割に、常用する流れではない |
| 入力欄の形 | **複数行** | 1行だとコピペした改行入りの文が潰れる。Enterは改行に使うので送信はCtrl+Enterとボタン |
| 自動返信の止め方 | **`get_active_keywords()` が空を返す** | 監視ループ側にif文を足す案は、ループの分岐が増えて読みにくい。セット/ペアの☑を直接OFFにする案は、ONに戻すとき元の組み合わせを復元できない |

### 途中で方針を変えた点

最初は「監視ループにON/OFFの分岐を足す」つもりで設計していたが、本人から

> すでにある仕組みを活用したいんだよね

と言われて見直した。調べると `get_active_keywords()` の呼び出し元は**監視ワーカー内の3箇所だけ**で、
UIからは呼ばれていなかった。監視ループは元から「有効キーワードが0件なら何もしない」作りなので、
**このメソッドに2行足すだけで自動返信が丸ごと止まる**。ループは1行も触っていない。

### 潜んでいた事故（先に塞いだもの）

#### 1. 自分が手入力した文へ自動返信してしまう

監視ループは自分の発言を `reply_message not in item[0]` で読み飛ばしていたが、
これは**登録済みの返信ワードにしか効かない**。手入力した文に検知ワードが混ざっていると、
自分の発言を検知して返信し、それがまた画面に出て…と延々続く。

- **直した内容**: `_is_own_manual_text()` を足し、既存の `_select_pending_replies()` の
  フィルタ条件に並べた。手入力で送った文は直近10件を覚えておく。
- **突き止めた手段**: `_select_pending_replies` を読み、除外条件が
  「返信ワードを含む行」だけであることを確認した。

#### 2. 改行を入れると文字が二重に入る

`_fill_reply_text` は貼り付け後に `reply_message[:15] in _current_input_text(d)` で
入ったか確かめ、入っていなければ直接入力に切り替える。
**アプリ側の入力欄が1行用だと改行が捨てられる**ため、この比較が外れて
「貼り付け失敗」と誤判定し、既に入っている文字の上からもう一度打ち込む。

- **直した内容**: `_normalize_for_compare()` / `_input_text_matches()` を足し、
  改行と空白を無視して比べるようにした。エンター送信後の確認も同じものに差し替え。
- **突き止めた手段**: 改行を許す変更を入れる前に、比較している箇所を読んで気付いた。

### 実測でわかったこと

#### `send_keys` は改行で誤送信しない（実装で確認）

`uiautomator2` の `send_keys` は、文字をbase64にして `ADB_KEYBOARD_INPUT_TEXT` を
ブロードキャストし、IMEが `commitText` で差し込む方式。
**`KEYCODE_ENTER` を打つ箇所が無い**ので、`\n` は「改行という文字」として入るだけ。

確かめ方:
```
python -c "import inspect;from uiautomator2 import _input;print(inspect.getsource(_input.InputMethodMixIn.send_keys))"
```
※ 日本語・中国語が混ざるので `PYTHONIOENCODING=utf-8` を付けないと cp932 で落ちる。

#### GRAVITYの入力欄は改行を保持する（実機で確認）

送信ボタンを押さずに、入れて→読み戻して→消す、で確かめた
（`scratchpad/test_live_newline.py`）。

| 項目 | 結果 |
|---|---|
| 入力欄の位置 | display=2 / `[28,1178][318,1241]` / 中心 (173,1209) |
| 打ち込んだ文字 | `'テスト1行目\nテスト2行目'` |
| 入力欄に入った文字 | `'テスト1行目\nテスト2行目'`（**改行そのまま**） |
| 誤送信 | 無し |

#### MuMuのadb接続先は変わっていた

memoryには `192.168.1.7:5555` と記録されていたが、今回は **`127.0.0.1:16416`**（インスタンス1）。
ポートを決め打ちせず `MuMuManager.exe info -v all` に問い合わせる設計にしてあるため、
コード側の変更は不要だった。**接続先の具体的な値は記録しても無駄になる。**

### 直した内容（まとめ）

| 場所 | 変更 |
|---|---|
| `KEYWORD_AUTO_REPLY_SETTING` | 設定キーの定数を追加 |
| `DatabaseManager.get_active_keywords()` | OFFなら空を返す（マスタースイッチ本体） |
| `AutoReplyWorker._log_active_keywords()` | 「OFFにしている」と「設定し忘れ」を区別して出す |
| `AutoReplyWorker.enqueue_manual_text()` | 手入力を送信待ちに積む受付口（新規） |
| `AutoReplyWorker._drain_manual_sends()` | 送信待ちを順に送り切る（新規） |
| `AutoReplyWorker._is_own_manual_text()` | 自分が手入力した行かを判定（新規） |
| `AutoReplyWorker._normalize_for_compare()` / `_input_text_matches()` | 改行・空白を無視して比べる（新規） |
| `AutoReplyWorker._select_pending_replies()` | 除外条件に手入力分を追加 |
| 監視ループ | 手入力を自動返信より先に送る。送ったら `continue` で読み直す |
| 監視ループ | 有効な検知ワードが無くなったら待機中の送信も取り消す |
| `App.build_main_tab()` | 「手入力して送る」枠とスイッチを追加 |
| `App.on_auto_reply_toggle()` / `send_manual_text()` / `_set_manual_send_enabled()` | 操作用（新規） |

### 設計上、後から見て迷いそうな点

- **手入力の送信を、なぜUIスレッドから直接やらないのか。**
  MuMuは画面を4枚同時に動かしていて座標が重なる。監視ループのタップと混ざると
  狙いと別の画面を触り、「設定アプリが勝手に開く」「ホームに戻る」が起きる。
  だから送信は必ず監視スレッド1本に通す（積むだけにして、送るのは監視スレッド）。
- **送信待ちを1件送るごとに画面を読み直す理由。**
  `nodes`（入力欄や送信ボタンの位置）は送信した瞬間に古くなる。
  2件目を古い `nodes` のまま送ると座標を外す。
- **送った文を「送る前」に覚える理由。**
  送信後に覚えると、その隙に監視ループが1周して自分の発言に返信してしまう。

### テスト

`scratchpad/test_manual_send.py`（27件・全通過）
`scratchpad/test_gui_widgets.py`（ウィジェット組み立てとCtrl+Enterの発火）
`scratchpad/test_live_newline.py`（実機・送信はしない）

⚠️ scratchpadはセッションの一時フォルダなので消える。
実際、v1.6.0までのテスト50本は既に消えていた。**残したいテストはリポジトリに入れること。**

### 未確認のまま残っていること

- 実際に送信ボタンまで押す通しテストは未実施（実在のルームへ投稿されるため）。
  本人が動かして確認する。
- 改行入りの文がGRAVITY側で1通として表示されるかは未確認
  （入力欄に改行が入るところまでは確認済み）。

---

## 2026-09-25（追記） — 置き場所の変更と呼び名の統一

### 指摘

> キーワードの自動返信オンオフはメインタブではなくキーワード設定に表示してあるべき。
> 手入力して送るも同様にキーワードタブに表示すべきだが、
> まず手入力して送るではなく手入力送信と名前を変更すべき

最初はメインタブ（開始/停止ボタンとログの間）に両方を置いていた。
「手で送る直前に自動返信を切る操作がセットになるので、監視の操作の近くが良い」
という判断だったが、**キーワードに関する操作はキーワード設定タブに集約する**方が筋が通る。
指摘どおり移設した。

### 直した内容

| 場所 | 変更 |
|---|---|
| `App.build_main_tab()` | 「手入力して送る」枠を丸ごと削除。メインタブは元の構成に戻した |
| `App.build_keywords_tab()` | 先頭に「キーワード自動返信」スイッチ、末尾に「手入力送信」枠を追加 |
| `App._refresh_auto_reply_hint()` | 新規。スイッチの効き方を言葉で添える |
| 呼び名 | 「手入力して送る」「手入力しての送信」→ **「手入力送信」** に統一（ログの文言も） |

スイッチをタブの先頭に置いたのは、**下に並ぶセットやペアの☑全体に掛かる**ものだから。
ただし見ただけでは掛かり方が分からないので、横に説明を出し分けるようにした。

- ON → 「☑ のペアが一致したら自動で返信します。」
- OFF → 「OFFの間は自動で返信しません（下の☑はそのまま保たれます）。」

### テストの作り直し（重要）

GUIテストが**ウィジェットをテスト側に書き写して**組み立てていた。
これでは本体を変えてもテストが通り続ける（`_select_pending_replies` を
関数へ切り出したときと同じ問題を、自分でやってしまっていた）。

`App.build_keywords_tab` を**本物のまま借りて呼ぶ**形に作り直した。
最小限の器（`Harness`）に `build_keywords_tab` / `_refresh_auto_reply_hint` /
`_set_manual_send_enabled` / `on_auto_reply_toggle` をクラス属性として借りている。

これにより「スイッチと入力欄がキーワードタブの中にあるか」「古い名前が残っていないか」
まで検証できるようになった（21件）。

### 実機テストについて

`test_live_newline.py` は GRAVITYの音声ルームが表示されていないと
「入力欄が見つかりません」で止まる。**これは失敗ではなく前提不足。**
改行の検証そのものは、ルームが出ていた時点で通過済み。

---

## 2026-09-25（追記2） — リリース前の検証で判明した「実在した不具合」

### 新機能だと思っていたものの中に、v1.6.0から存在する不具合が混ざっていた

リリース手順に沿って「修正前のコードで回帰テストが落ちること」を確かめようとしたとき、
今回の変更を **新機能／既存の不具合修正** に分ける必要が出た。分けた結果:

| 変更 | 分類 | v1.6.0で到達できたか |
|---|---|---|
| キーワード自動返信のON/OFF | 新機能 | — |
| 手入力送信 | 新機能 | — |
| 自分の手入力文への自動返信を防ぐ | 新機能の一部（事故の予防） | 手入力送信が無いので到達不可 |
| **改行を捨てる入力欄で文字が二重に入る** | **既存の不具合** | **到達可能（後述）** |
| 有効な検知ワードが無くなった時の待機送信の取り消し | **既存の不具合** | **到達可能（後述）** |

### 不具合A: 改行を捨てる入力欄で文字が二重に入る

- **症状**: 送信される文が「あいさつおしらせあいさつおしらせ」のように二重になる。
- **原因**: `_fill_reply_text` は貼り付け後に
  `reply_message[:15] in _current_input_text(d)` の**厳密一致**で確認する。
  アプリの入力欄が1行用(`android:singleLine`)だと Android が改行を捨てるため、
  貼り付けは成功しているのに一致せず、「失敗した」とみなして
  `_type_reply_text` で**もう一度打ち込む**。
- **v1.6.0でも到達可能**: 返信ワードは1行のCTkEntryからしか来ないが、
  `_fill_reply_text` の判定そのものは v1.6.0 に同じ形で存在する
  （`git show v1.6.0:files/auto_reply_app.py` の 4494行・4619行）。
  改行を含む返信ワードを作る経路が無かったため表面化していなかっただけ。
  v1.7.0で複数行の手入力送信を足すと**確実に踏む**。
- **対応の種別**: **根本原因の修正**。不正な判定をしている箇所そのものを直した。
- **直した内容**: `_normalize_for_compare()` / `_input_text_matches()` を足し、
  改行と空白を無視して比べる。貼り付け後の確認とエンター送信後の確認の両方を差し替え。
- **同じ原因の他の箇所**: `reply_message[:15]` での比較は**2箇所だけ**
  （`_fill_reply_text` と `_send_reply`）。両方とも修正対象に含めた。

**修正前の実測**（v1.6.0のワークツリーで `tests/test_regression_newline_paste.py` を実行）:

```
[1] 改行を捨てる入力欄へ、改行入りの文を入れる
  ok   入力は成功したと返す
  FAIL 入力欄の中身が二重になっていない
       '1行目のあいさつ2行目のおしらせ1行目のあいさつ2行目のおしらせ'
  FAIL 貼り付けが効いたのに直接入力へ切り替えていない
       ['1行目のあいさつ\n2行目のおしらせ']
  FAIL この端末では貼り付けが使えると判断したまま  False
```

同じ実行で [2]（改行を保持する入力欄）と [3]（貼り付けが効かない端末）は
**v1.6.0でも通っている**。つまりこのテストは既存の正しい挙動を巻き込まず、
狙った不具合だけを捕らえている。修正後は全10件が通る。

### 不具合B: 有効な検知ワードが無くなっても、待機中の返信が送られる

- **症状**: キーワードセットを無効にしたのに、その直後に返信が1件送られる。
- **原因**: クールダウン待ちの `pending_sends` は `keywords_map` とは independent に
  処理される。セットを無効にすると `keywords_map` は空になるが、
  `pending_sends` に残った分はそのまま送信される。
  しかも `cooldown_enabled` が偽なら「OFFになったので即座に送信」の経路に入る。
- **v1.6.0でも到達可能**: クールダウン待ちが発生している最中にセットを無効化すれば起きる。
- **対応の種別**: **根本原因の修正**（`keywords_map` が空なら `pending_sends` を捨てる）。
- ⚠️ **回帰テストは追加できていない。** この判定は `_run_monitor` の
  whileループ本体に直書きされており、ループを回さずに検証できない。
  ループを分解するのは今回の依頼の範囲を超える（無関係なリファクタリングになる）。
  **代わりに実施した検証**: `get_active_keywords()` がOFFで `{}` を返すことを
  `tests/test_manual_send.py` と `tests/test_gui_widgets.py` で確認し、
  ループ側は `keywords_map` が空なら `pending_sends.clear()` する1経路のみであることを
  差分で確認した。実挙動の確認は実機での操作に依存する。

### この検証で学んだこと

**「新機能を足しただけ」と思っていても、既存コードの判定を新しい入力に晒すと
そこに潜んでいた不具合が到達可能になる。** 今回は複数行入力を足したことで、
1行しか来ない前提で書かれていた比較が壊れた。
新しい種類の入力を通す変更をするときは、**その入力が通る既存の判定を洗い出す**こと。
